# Claude Compatibility Stub

The plotwell AI harness has moved from this long `CLAUDE.md` file to a folder-based agent guide:

- Start with `AGENTS.md`.
- Read focused docs in `.agents/docs/`.
- Use reusable role prompts from `.agents/agents/`.
- Use installed workflows from `.agents/skills/`.

This file remains only for tools that still look for `CLAUDE.md`. Keep new durable instructions in the AGENTS-based harness.
