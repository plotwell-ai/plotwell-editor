import { EditorState as e, Plugin as t, PluginKey as n } from "prosemirror-state";
import { Decoration as r, DecorationSet as i, EditorView as a } from "prosemirror-view";
import { history as o, redo as s, undo as c } from "prosemirror-history";
import { keymap as l } from "prosemirror-keymap";
import { baseKeymap as u, toggleMark as d } from "prosemirror-commands";
import { Node as f, Schema as p } from "prosemirror-model";
import { InputRule as m, inputRules as h } from "prosemirror-inputrules";
//#region src/schema/index.ts
var g = new p({
	nodes: {
		doc: { content: "block+" },
		sceneHeading: {
			group: "block",
			content: "text*",
			defining: !0,
			attrs: { id: { default: null } },
			toDOM: () => [
				"p",
				{
					class: "pm-scene-heading",
					"data-type": "scene-heading",
					"data-placeholder": "INT. SCENE - DAY"
				},
				0
			],
			parseDOM: [{ tag: "p[data-type=\"scene-heading\"]" }]
		},
		action: {
			group: "block",
			content: "text*",
			defining: !0,
			toDOM: () => [
				"p",
				{
					class: "pm-action",
					"data-type": "action",
					"data-placeholder": "Action..."
				},
				0
			],
			parseDOM: [{ tag: "p[data-type=\"action\"]" }]
		},
		character: {
			group: "block",
			content: "text*",
			defining: !0,
			toDOM: () => [
				"p",
				{
					class: "pm-character",
					"data-type": "character",
					"data-placeholder": "CHARACTER"
				},
				0
			],
			parseDOM: [{ tag: "p[data-type=\"character\"]" }]
		},
		dialogue: {
			group: "block",
			content: "text*",
			defining: !0,
			toDOM: () => [
				"p",
				{
					class: "pm-dialogue",
					"data-type": "dialogue",
					"data-placeholder": "Dialogue..."
				},
				0
			],
			parseDOM: [{ tag: "p[data-type=\"dialogue\"]" }]
		},
		parenthetical: {
			group: "block",
			content: "text*",
			defining: !0,
			toDOM: () => [
				"p",
				{
					class: "pm-parenthetical",
					"data-type": "parenthetical",
					"data-placeholder": "beat"
				},
				0
			],
			parseDOM: [{ tag: "p[data-type=\"parenthetical\"]" }]
		},
		transition: {
			group: "block",
			content: "text*",
			defining: !0,
			toDOM: (e) => [
				"p",
				{
					class: "pm-transition" + (e.textContent.toUpperCase().includes("FADE IN") ? " pw-fade-in" : ""),
					"data-type": "transition",
					"data-placeholder": "CUT TO:"
				},
				0
			],
			parseDOM: [{ tag: "p[data-type=\"transition\"]" }]
		},
		pageBreak: {
			group: "block",
			atom: !0,
			toDOM: () => ["div", {
				class: "pm-page-break",
				"data-type": "page-break",
				contenteditable: "false"
			}],
			parseDOM: [{ tag: "div[data-type=\"page-break\"]" }]
		},
		text: { group: "inline" }
	},
	marks: {
		bold: {
			toDOM: () => ["strong", 0],
			parseDOM: [{ tag: "strong" }, { tag: "b" }]
		},
		italic: {
			toDOM: () => ["em", 0],
			parseDOM: [{ tag: "em" }, { tag: "i" }]
		},
		underline: {
			toDOM: () => ["u", 0],
			parseDOM: [{ tag: "u" }]
		}
	}
}), _ = {
	sceneHeading: "action",
	action: "action",
	character: "dialogue",
	dialogue: "action",
	parenthetical: "dialogue",
	transition: "sceneHeading"
}, v = [
	"action",
	"character",
	"sceneHeading",
	"transition",
	"parenthetical"
];
function y(e) {
	let { $from: t } = e.selection;
	return t.parent.type.name;
}
var ee = (e, t) => {
	let { $from: n, $to: r } = e.selection;
	if (!n.sameParent(r)) return !1;
	let i = y(e);
	if (!i) return !1;
	let a = _[i] ?? "action", o = g.nodes[a];
	if (!o) return !1;
	let s = n.parent, c = s.content.size > 0, l = n.parentOffset === 0;
	if (n.parentOffset, s.content.size, !c && i !== "action") {
		if (t) {
			let r = n.before();
			t(e.tr.setNodeMarkup(r, g.nodes.action).scrollIntoView());
		}
		return !0;
	}
	if (!c) return t && t(e.tr.split(n.pos, 1, [{ type: g.nodes.action }]).scrollIntoView()), !0;
	if (l && c) {
		if (t) {
			let r = n.before(), i = g.nodes.action.create(), a = e.tr.insert(r, i), o = e.selection.constructor;
			a = a.setSelection(o.near(a.doc.resolve(r + i.nodeSize + 1))), t(a.scrollIntoView());
		}
		return !0;
	}
	return t && t(e.tr.split(n.pos, 1, [{ type: o }]).scrollIntoView()), !0;
}, te = (e, t) => {
	let n = y(e);
	if (!n) return !1;
	let r = v[(v.indexOf(n) + 1) % v.length], i = g.nodes[r];
	if (!i) return !1;
	if (t) {
		let { from: n, to: r } = e.selection;
		t(e.tr.setBlockType(n, r, i));
	}
	return !0;
}, b = (e, t) => {
	let n = y(e);
	if (!n) return !1;
	let r = v[(v.indexOf(n) - 1 + v.length) % v.length], i = g.nodes[r];
	if (!i) return !1;
	if (t) {
		let { from: n, to: r } = e.selection;
		t(e.tr.setBlockType(n, r, i));
	}
	return !0;
}, ne = (e, t) => {
	let { $from: n, empty: r } = e.selection;
	if (!r || n.parentOffset !== 0) return !1;
	let i = y(e);
	if (!i || i === "action") return !1;
	let a = n.parent, o = n.before(), s = e.doc.resolve(o).nodeBefore;
	if (!s) return !0;
	if (s.content.size === 0) return t && t(e.tr.delete(o - s.nodeSize, o).scrollIntoView()), !0;
	if (a.content.size === 0) {
		if (t) {
			let n = o + a.nodeSize, r = e.tr.delete(o, n), i = e.selection.constructor.near(r.doc.resolve(o - 1), -1);
			t(r.setSelection(i).scrollIntoView());
		}
		return !0;
	}
	if (t) {
		let n = o - 1, r = e.selection.constructor.near(e.tr.doc.resolve(n), -1);
		t(e.tr.setSelection(r).scrollIntoView());
	}
	return !0;
};
function x(e) {
	return (t, n) => {
		let r = g.nodes[e];
		if (!r) return !1;
		if (n) {
			let { from: e, to: i } = t.selection;
			n(t.tr.setBlockType(e, i, r));
		}
		return !0;
	};
}
var S = l({
	Enter: ee,
	Tab: te,
	"Shift-Tab": b,
	Backspace: ne,
	"Mod-b": d(g.marks.bold),
	"Mod-i": d(g.marks.italic),
	"Mod-u": d(g.marks.underline),
	"Mod-1": x("sceneHeading"),
	"Mod-2": x("action"),
	"Mod-3": x("character"),
	"Mod-4": x("dialogue"),
	"Mod-5": x("parenthetical"),
	"Mod-6": x("transition")
}), re = h({ rules: [new m(/^(INT\.|EXT\.|INT\/EXT\.|I\/E\.)\s$/, (e, t, n, r) => e.doc.resolve(n).parent.type === g.nodes.sceneHeading ? null : e.tr.setBlockType(n, r, g.nodes.sceneHeading)), new m(/^(FADE OUT\.|FADE IN:|CUT TO:|SMASH CUT TO:|DISSOLVE TO:)\s$/, (e, t, n, r) => e.doc.resolve(n).parent.type === g.nodes.transition ? null : e.tr.setBlockType(n, r, g.nodes.transition))] }), C = 96, w = 864, T = new n("pagination"), E = new Set(["character"]), D = new Set(["dialogue", "parenthetical"]), ie = new t({
	key: T,
	state: {
		init: () => i.empty,
		apply(e, t) {
			return e.getMeta(T) ?? t.map(e.mapping, e.doc);
		}
	},
	props: { decorations(e) {
		return T.getState(e);
	} },
	view(e) {
		let t = null, n = "";
		function a() {
			if (!e.dom || !e.dom.parentNode) return;
			let { state: t } = e, a = [];
			t.doc.forEach((t, n) => {
				if (t.type.name === "pageBreak") return;
				let r;
				try {
					r = e.nodeDOM(n);
				} catch {
					return;
				}
				if (!r) return;
				let i = window.getComputedStyle(r), o = parseFloat(i.marginTop) || 0, s = parseFloat(i.marginBottom) || 0, c = r.offsetHeight + o + s;
				a.push({
					pos: n,
					height: c,
					typeName: t.type.name
				});
			});
			let o = [], s = 1, c = 0;
			for (let e = 0; e < a.length; e++) {
				let t = a[e];
				if (c + t.height > w && c > 0) {
					let n = e;
					if (D.has(t.typeName) && e > 0) {
						let t = e - 1;
						for (; t >= 0 && D.has(a[t].typeName);) t--;
						t >= 0 && a[t].typeName === "character" && (n = t);
					}
					E.has(t.typeName) && (n = e), n <= 0 && (n = e), s++, o.push(a[n].pos), c = 0;
					for (let t = n; t <= e; t++) c += a[t].height;
				} else c += t.height;
			}
			let l = JSON.stringify(o);
			if (l === n) return;
			n = l;
			let u = [];
			o.forEach((e, t) => {
				let n = t + 2;
				u.push(r.widget(e, () => {
					let e = document.createElement("div");
					e.className = "pw-page-separator", e.contentEditable = "false";
					let t = document.createElement("div");
					t.className = "pw-page-margin-bottom", e.appendChild(t);
					let r = document.createElement("div");
					r.className = "pw-page-margin-top";
					let i = document.createElement("span");
					return i.className = "pw-page-header-number", i.textContent = `${n}.`, r.appendChild(i), e.appendChild(r), e;
				}, {
					side: -1,
					key: `page-${n}`
				}));
			});
			let d = w - c, f = Math.max(0, d);
			e.dom.style.paddingBottom = `${C + f}px`;
			let p = i.create(t.doc, u), m = t.tr.setMeta(T, p);
			m.setMeta("addToHistory", !1), e.dispatch(m);
		}
		function o() {
			t !== null && cancelAnimationFrame(t), t = requestAnimationFrame(() => {
				t = null, a();
			});
		}
		return requestAnimationFrame(() => o()), {
			update(e, t) {
				e.state.doc !== t.doc && o();
			},
			destroy() {
				t !== null && cancelAnimationFrame(t);
			}
		};
	}
}), ae = new n("bubbleMenu"), oe = [
	{
		mark: "bold",
		label: "Bold",
		icon: "<strong>B</strong>"
	},
	{
		mark: "italic",
		label: "Italic",
		icon: "<em>I</em>"
	},
	{
		mark: "underline",
		label: "Underline",
		icon: "<u>U</u>"
	}
];
function se(e, t) {
	let { from: n, $from: r, to: i, empty: a } = e.state.selection, o = g.marks[t];
	return a ? !!o.isInSet(e.state.storedMarks || r.marks()) : e.state.doc.rangeHasMark(n, i, o);
}
function ce() {
	let e = null;
	function n(e) {
		let t = document.createElement("div");
		return t.className = "pw-bubble-menu", oe.forEach((n, i) => {
			if (i > 0) {
				let e = document.createElement("span");
				e.className = "pw-separator", t.appendChild(e);
			}
			let a = document.createElement("button");
			a.innerHTML = n.icon, a.title = n.label, a.setAttribute("data-mark", n.mark), a.addEventListener("mousedown", (i) => {
				i.preventDefault(), i.stopPropagation();
				let a = g.marks[n.mark];
				d(a)(e.state, e.dispatch), e.focus(), r(e, t);
			}), t.appendChild(a);
		}), t;
	}
	function r(e, t) {
		t.querySelectorAll("button[data-mark]").forEach((t) => {
			let n = t.getAttribute("data-mark");
			t.classList.toggle("active", se(e, n));
		});
	}
	function i(e, t) {
		let { from: n, to: r } = e.state.selection, i = e.coordsAtPos(n), a = e.coordsAtPos(r), o = e.dom.getBoundingClientRect(), s = t.getBoundingClientRect(), c = (i.left + a.right) / 2 - o.left - s.width / 2, l = i.top - o.top - s.height - 8;
		c = Math.max(4, Math.min(c, o.width - s.width - 4)), t.style.left = `${c}px`, t.style.top = `${l}px`;
	}
	return new t({
		key: ae,
		view(t) {
			e = n(t), e.style.display = "none";
			let a = t.dom.parentElement;
			return a && a.appendChild(e), {
				update(t) {
					let { empty: n, from: a, to: o } = t.state.selection;
					if (n || a === o) {
						e.style.display = "none";
						return;
					}
					e.style.display = "flex", r(t, e), requestAnimationFrame(() => {
						e && e.style.display !== "none" && i(t, e);
					});
				},
				destroy() {
					e?.remove(), e = null;
				}
			};
		}
	});
}
//#endregion
//#region src/plugins/typeBadge.ts
var le = new n("typeBadge"), O = [
	{
		type: "sceneHeading",
		label: "Scene Heading",
		shortcut: "⌘ 1"
	},
	{
		type: "action",
		label: "Action",
		shortcut: "⌘ 2"
	},
	{
		type: "character",
		label: "Character",
		shortcut: "⌘ 3"
	},
	{
		type: "dialogue",
		label: "Dialogue",
		shortcut: "⌘ 4"
	},
	{
		type: "parenthetical",
		label: "Parenthetical",
		shortcut: "⌘ 5"
	},
	{
		type: "transition",
		label: "Transition",
		shortcut: "⌘ 6"
	}
];
function ue(e) {
	return e.state.selection.$from.parent.type.name;
}
function de(e, t) {
	let n = g.nodes[t];
	if (!n) return;
	let { from: r, to: i } = e.state.selection;
	e.dispatch(e.state.tr.setBlockType(r, i, n)), e.focus();
}
function k() {
	let e = null, n = null, r = !1;
	function i(e) {
		let t = document.createElement("div");
		t.className = "pw-type-badge";
		let n = document.createElement("button");
		return n.className = "pw-type-badge-label", n.addEventListener("mousedown", (t) => {
			t.preventDefault(), t.stopPropagation(), o(e);
		}), t.appendChild(n), t;
	}
	function a(e) {
		let t = document.createElement("div");
		return t.className = "pw-type-dropdown", O.forEach((n) => {
			let r = document.createElement("button");
			r.setAttribute("data-type", n.type);
			let i = document.createElement("span");
			i.textContent = n.label;
			let a = document.createElement("span");
			a.className = "pw-shortcut", a.textContent = n.shortcut, r.appendChild(i), r.appendChild(a), r.addEventListener("mousedown", (t) => {
				t.preventDefault(), t.stopPropagation(), de(e, n.type), l();
			}), t.appendChild(r);
		}), t;
	}
	function o(e) {
		r ? l() : s(e);
	}
	function s(t) {
		e && (n && n.remove(), n = a(t), e.appendChild(n), r = !0, u(), c && document.removeEventListener("mousedown", c), c = (t) => {
			e?.contains(t.target) || l();
		}, setTimeout(() => document.addEventListener("mousedown", c), 0));
	}
	let c = null;
	function l() {
		n?.remove(), n = null, r = !1, c &&= (document.removeEventListener("mousedown", c), null);
	}
	function u() {
		if (!n || !e) return;
		let t = e.getAttribute("data-current-type");
		n.querySelectorAll("button").forEach((e) => {
			e.classList.toggle("active", e.getAttribute("data-type") === t);
		});
	}
	function d(t) {
		if (!e) return;
		let { $from: n } = t.state.selection, r = n.before(n.depth), i = t.coordsAtPos(r + 1), a = t.dom.getBoundingClientRect(), o = i.top - a.top;
		e.style.left = "-120px", e.style.top = `${o}px`;
	}
	function f(t) {
		if (!e) return;
		let n = ue(t), i = O.find((e) => e.type === n);
		if (!i) {
			e.style.display = "none";
			return;
		}
		e.style.display = "flex", e.setAttribute("data-current-type", n);
		let a = e.querySelector(".pw-type-badge-label");
		a && (a.textContent = i.label), d(t), r && u();
	}
	return new t({
		key: le,
		view(t) {
			return e = i(t), e.style.display = "none", t.dom.parentElement.appendChild(e), requestAnimationFrame(() => f(t)), {
				update(e) {
					requestAnimationFrame(() => f(e));
				},
				destroy() {
					l(), e?.remove(), e = null;
				}
			};
		}
	});
}
//#endregion
//#region src/plugins/sceneNumbers.ts
var A = new n("sceneNumbers"), j = new t({
	key: A,
	state: {
		init(e, t) {
			return M(t.doc);
		},
		apply(e, t) {
			return e.docChanged ? M(e.doc) : t.map(e.mapping, e.doc);
		}
	},
	props: { decorations(e) {
		return A.getState(e);
	} }
});
function M(e) {
	let t = [], n = 0;
	return e.forEach((e, i) => {
		if (e.type.name === "sceneHeading") {
			n++;
			let e = String(n);
			t.push(r.widget(i + 1, () => {
				let t = document.createElement("span");
				return t.className = "pw-scene-number pw-scene-number-left", t.textContent = e, t.contentEditable = "false", t;
			}, {
				side: -1,
				key: `sn-l-${i}-${e}`
			})), t.push(r.widget(i + 1, () => {
				let t = document.createElement("span");
				return t.className = "pw-scene-number pw-scene-number-right", t.textContent = e, t.contentEditable = "false", t;
			}, {
				side: 1,
				key: `sn-r-${i}-${e}`
			}));
		}
	}), i.create(e, t);
}
//#endregion
//#region src/plugins/fadeInAlign.ts
var N = new n("fadeInAlign");
function P() {
	return new t({
		key: N,
		view() {
			return { update(e) {
				e.dom.querySelectorAll(".pm-transition").forEach((e) => {
					let t = (e.textContent || "").toUpperCase().trim();
					e.classList.toggle("pw-fade-in", /^FADE\s+IN[.:]?$/.test(t));
				});
			} };
		}
	});
}
//#endregion
//#region src/importers/fountain.ts
var F = new Set([
	"FADE OUT.",
	"FADE IN:",
	"FADE TO BLACK.",
	"CUT TO:",
	"SMASH CUT TO:",
	"MATCH CUT TO:",
	"DISSOLVE TO:",
	"JUMP CUT TO:",
	"WIPE TO:",
	"TIME CUT:",
	"IRIS IN:",
	"IRIS OUT."
]);
function I(e) {
	let t = e.trim().toUpperCase();
	return !!(F.has(t) || /^[A-Z\s]+TO:$/.test(t));
}
function L(e) {
	let t = e.trim();
	return !!(/^(INT\.|EXT\.|INT\.\/EXT\.|INT\/EXT\.|I\/E\.)\s/i.test(t) || /^\.[A-Z]/.test(t));
}
function R(e, t) {
	let n = e.trim();
	return !(!/^[A-Z][A-Z\s\.\-']+(\s*\([\w\s\.\']+\))?$/.test(n) || I(e) || /^(INT|EXT|FADE|CUT|DISSOLVE|SMASH|MATCH|WIPE|JUMP|IRIS|TIME)$/.test(n) || !t || t.trim() === "" || n.length < 2);
}
function z(e) {
	let t = e.split("\n"), n = [], r = 0;
	for (; r < t.length;) {
		let e = t[r].trimEnd();
		if (e.trim() === "") {
			r++;
			continue;
		}
		if (L(e)) {
			let t = e.trim().replace(/^\./, "").toUpperCase();
			n.push({
				type: "sceneHeading",
				text: t
			}), r++;
			continue;
		}
		if (I(e)) {
			n.push({
				type: "transition",
				text: e.trim().toUpperCase()
			}), r++;
			continue;
		}
		if (e.startsWith(">") && !e.endsWith("<")) {
			n.push({
				type: "transition",
				text: e.slice(1).trim()
			}), r++;
			continue;
		}
		if (R(e, t[r + 1])) {
			for (n.push({
				type: "character",
				text: e.trim()
			}), r++; r < t.length && t[r].trim() !== "";) {
				let e = t[r].trimEnd();
				if (/^\s*\(.*\)\s*$/.test(e)) {
					let t = e.trim().replace(/^\(/, "").replace(/\)$/, "");
					n.push({
						type: "parenthetical",
						text: t
					});
				} else n.push({
					type: "dialogue",
					text: e.trim()
				});
				r++;
			}
			continue;
		}
		for (; r < t.length && t[r].trim() !== "";) n.push({
			type: "action",
			text: t[r].trimEnd()
		}), r++;
	}
	return n;
}
function B(e) {
	let t = z(e).map((e) => {
		let t = g.nodes[e.type], n = e.text ? g.text(e.text) : void 0;
		return t.create({}, n);
	});
	return t.length === 0 && t.push(g.nodes.action.create()), g.nodes.doc.create({}, t);
}
//#endregion
//#region src/importers/fdx.ts
var V = {
	"Scene Heading": "sceneHeading",
	Action: "action",
	Character: "character",
	Dialogue: "dialogue",
	Parenthetical: "parenthetical",
	Transition: "transition",
	General: "action",
	Shot: "sceneHeading"
};
function H(e) {
	let t = [], n = e.getAttribute("Style") || "";
	return n.includes("Bold") && t.push(g.marks.bold.create()), n.includes("Italic") && t.push(g.marks.italic.create()), n.includes("Underline") && t.push(g.marks.underline.create()), t;
}
function fe(e) {
	let t = e.querySelectorAll("Text"), n = [];
	return t.forEach((e) => {
		let t = e.textContent || "";
		if (!t) return;
		let r = H(e);
		n.push(g.text(t, r));
	}), n;
}
function U(e) {
	let t = new DOMParser().parseFromString(e, "application/xml"), n = t.querySelector("parsererror");
	if (n) throw Error(`Invalid FDX file: ${n.textContent}`);
	let r = t.querySelectorAll("Content > Paragraph"), i = [];
	return r.forEach((e) => {
		let t = V[e.getAttribute("Type") || "Action"] || "action", n = g.nodes[t];
		if (!n) return;
		let r = fe(e);
		if (t === "parenthetical" && r.length === 1) {
			let e = r[0].text || "";
			if (e.startsWith("(") && e.endsWith(")")) {
				let t = e.slice(1, -1);
				t ? i.push(n.create({}, g.text(t))) : i.push(n.create());
				return;
			}
		}
		r.length > 0 ? i.push(n.create({}, r)) : i.push(n.create());
	}), i.length === 0 && i.push(g.nodes.action.create()), g.nodes.doc.create({}, i);
}
//#endregion
//#region src/importers/scenes.ts
function W(e) {
	let t = [];
	for (let n of e) {
		if (n.heading) {
			let e = n.heading.toUpperCase();
			t.push(g.nodes.sceneHeading.create({}, g.text(e)));
		}
		if (n.action_content) {
			let e = z(n.action_content);
			for (let n of e) {
				let e = g.nodes[n.type], r = n.text ? g.text(n.text) : void 0;
				t.push(e.create({}, r));
			}
		}
	}
	return t.length === 0 && t.push(g.nodes.action.create()), g.nodes.doc.create({}, t);
}
//#endregion
//#region src/importers/tiptap.ts
var pe = {
	"scene-heading": "sceneHeading",
	action: "action",
	"character-name": "character",
	dialogue: "dialogue",
	parenthetical: "parenthetical",
	transition: "transition",
	"transition aligned": "transition",
	hook: "action",
	voiceover: "action",
	"shot-description": "action",
	cta: "action",
	"music-cue": "action",
	"text-overlay": "action",
	"timing-note": "action"
}, me = {
	sceneHeading: "scene-heading",
	action: "action",
	character: "character-name",
	dialogue: "dialogue",
	parenthetical: "parenthetical",
	transition: "transition"
}, he = new Set([
	"bold",
	"italic",
	"underline"
]);
function ge(e) {
	if (!e || e.length === 0) return;
	let t = e.filter((e) => he.has(e.type));
	return t.length > 0 ? t : void 0;
}
function _e(e) {
	if (!e || !Array.isArray(e)) return [];
	let t = [];
	for (let n of e) if (n.type === "text" && n.text) {
		let e = ge(n.marks), r = e ? e.map((e) => g.marks[e.type].create(e.attrs)) : void 0;
		t.push(g.text(n.text, r));
	} else n.type;
	return t;
}
function G(e) {
	if (!e || e.type !== "doc" || !Array.isArray(e.content)) return g.nodes.doc.create({}, [g.nodes.action.create()]);
	let t = [];
	for (let n of e.content) if (!(!n || typeof n != "object")) {
		if (n.type !== "paragraph" && n.type !== "heading" && g.nodes[n.type]) {
			try {
				t.push(f.fromJSON(g, n));
			} catch {}
			continue;
		}
		if (n.type === "paragraph") {
			let e = pe[n.attrs?.class || "action"] || "action", r = g.nodes[e];
			if (!r) continue;
			let i = _e(n.content), a = e === "sceneHeading" ? { id: n.attrs?.sceneId || null } : {};
			t.push(r.create(a, i.length > 0 ? i : void 0));
			continue;
		}
	}
	return t.length === 0 && t.push(g.nodes.action.create()), g.nodes.doc.create({}, t);
}
function ve(e) {
	let t = [];
	return e.forEach((e) => {
		let n = me[e.type.name];
		if (!n) return;
		let r = [];
		e.forEach((e) => {
			if (e.isText) {
				let t = {
					type: "text",
					text: e.text
				};
				e.marks.length > 0 && (t.marks = e.marks.map((e) => {
					let t = { type: e.type.name };
					return Object.keys(e.attrs).length > 0 && (t.attrs = e.attrs), t;
				})), r.push(t);
			}
		});
		let i = {
			type: "paragraph",
			attrs: { class: n }
		};
		r.length > 0 && (i.content = r), t.push(i);
	}), {
		type: "doc",
		content: t
	};
}
//#endregion
//#region src/exporters/fountain.ts
function K(e) {
	let t = [];
	return e.forEach((e) => {
		let n = e.type.name, r = e.textContent;
		switch (n) {
			case "sceneHeading":
				t.length > 0 && t.push(""), t.push(r.toUpperCase());
				break;
			case "action":
				t.length > 0 && t.push(""), t.push(r);
				break;
			case "character":
				t.length > 0 && t.push(""), t.push(r.toUpperCase());
				break;
			case "dialogue":
				t.push(r);
				break;
			case "parenthetical":
				t.push(`(${r})`);
				break;
			case "transition":
				t.length > 0 && t.push(""), t.push(r.toUpperCase());
				break;
			case "pageBreak":
				t.push(""), t.push("===");
				break;
			default:
				r && t.push(r);
				break;
		}
	}), t.join("\n") + "\n";
}
//#endregion
//#region src/exporters/fdx.ts
var ye = {
	sceneHeading: "Scene Heading",
	action: "Action",
	character: "Character",
	dialogue: "Dialogue",
	parenthetical: "Parenthetical",
	transition: "Transition"
};
function q(e) {
	return e.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&apos;");
}
function be(e) {
	if (e.childCount === 0) return "<Text></Text>";
	let t = [];
	return e.forEach((e) => {
		let n = q(e.text || ""), r = [];
		e.marks.forEach((e) => {
			switch (e.type.name) {
				case "bold":
					r.push("Bold");
					break;
				case "italic":
					r.push("Italic");
					break;
				case "underline":
					r.push("Underline");
					break;
			}
		});
		let i = r.length > 0 ? ` Style="${r.join("+")}"` : "";
		t.push(`<Text${i}>${n}</Text>`);
	}), t.join("");
}
function J(e) {
	let t = [];
	return e.forEach((e) => {
		let n = e.type.name;
		if (n === "pageBreak") return;
		let r = ye[n];
		if (!r) return;
		let i;
		i = n === "parenthetical" ? `<Text>(${q(e.textContent)})</Text>` : be(e), t.push(`    <Paragraph Type="${r}">\n      ${i}\n    </Paragraph>`);
	}), `<?xml version="1.0" encoding="UTF-8"?>
<FinalDraft DocumentType="Script" Template="No" Version="4">
  <Content>
${t.join("\n")}
  </Content>
</FinalDraft>
`;
}
//#endregion
//#region src/exporters/pdf.ts
function Y(e) {
	let t = [], n = 0;
	return e.forEach((e) => {
		let r = e.type.name;
		if (r === "pageBreak") return;
		let i = xe(e);
		switch (r) {
			case "sceneHeading":
				n++, t.push(`<p class="pm-scene-heading" data-type="scene-heading"><span class="pw-sn pw-sn-l">${n}</span>${i}<span class="pw-sn pw-sn-r">${n}</span></p>`);
				break;
			case "action":
				t.push(`<p class="pm-action" data-type="action">${i}</p>`);
				break;
			case "character":
				t.push(`<p class="pm-character" data-type="character">${i}</p>`);
				break;
			case "dialogue":
				t.push(`<p class="pm-dialogue" data-type="dialogue">${i}</p>`);
				break;
			case "parenthetical":
				t.push(`<p class="pm-parenthetical" data-type="parenthetical">${i}</p>`);
				break;
			case "transition": {
				let n = e.textContent.toUpperCase().includes("FADE IN") ? " pw-fade-in" : "";
				t.push(`<p class="pm-transition${n}" data-type="transition">${i}</p>`);
				break;
			}
			default: i && t.push(`<p class="pm-action" data-type="action">${i}</p>`);
		}
	}), t.join("\n");
}
function xe(e) {
	if (e.childCount === 0) return "";
	let t = [];
	return e.forEach((e) => {
		let n = X(e.text || "");
		e.marks.forEach((e) => {
			switch (e.type.name) {
				case "bold":
					n = `<strong>${n}</strong>`;
					break;
				case "italic":
					n = `<em>${n}</em>`;
					break;
				case "underline":
					n = `<u>${n}</u>`;
					break;
			}
		}), t.push(n);
	}), t.join("");
}
function X(e) {
	return e.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
}
function Z(e, t) {
	return `<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>${X(t || "Screenplay")}</title>
<style>
@import url('https://fonts.googleapis.com/css2?family=Courier+Prime:ital,wght@0,400;0,700;1,400&display=swap');

/* ---- Page setup for print ---- */
@page {
  size: 8.5in 11in;
  margin: 1in 1in 1in 1in;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: "Courier Prime", "Courier New", Courier, monospace;
  font-size: 12pt;
  line-height: 16px;
  color: #000;
  -webkit-print-color-adjust: exact;
  print-color-adjust: exact;
}

p {
  margin: 0;
  padding: 0;
  line-height: 16px;
  orphans: 2;
  widows: 2;
  position: relative;
}

/* ---- Screenplay elements (identical to editor) ---- */

.pm-scene-heading {
  text-transform: uppercase;
  font-weight: bold;
  width: 6in;
  page-break-after: avoid;
  position: relative;
}

.page > p:first-child {
  margin-top: 0 !important;
}

.pm-action {
  width: 6in;
  margin-top: 16px;
}

.pm-character {
  margin-left: 2.2in;
  width: 3.8in;
  text-transform: uppercase;
  margin-top: 16px;
  page-break-after: avoid;
}

.pm-dialogue + .pm-character,
.pm-parenthetical + .pm-character {
  margin-top: 8px; /* tighter after dialogue - matches editor */
}

.pm-dialogue {
  margin-left: 1in;
  width: 3.5in;
  page-break-before: avoid;
}

.pm-parenthetical {
  margin-left: 1.6in;
  width: 2.3in;
  page-break-before: avoid;
  page-break-after: avoid;
}

.pm-parenthetical::before { content: "("; }
.pm-parenthetical::after  { content: ")"; }

.pm-transition {
  text-align: right;
  text-transform: uppercase;
  width: 6in;
  margin-top: 16px;
}

.pm-transition.pw-fade-in {
  text-align: left;
}

/* ---- Scene Numbers (print-safe) ---- */
.pm-scene-heading {
  margin-top: 48px;
  margin-bottom: 16px;
  overflow: visible !important;
}
.pm-scene-heading + .pm-action {
  margin-top: 0;
}
.pw-sn {
  position: absolute;
  top: 0;
  font-weight: bold;
  line-height: 16px;
}
.pw-sn-l {
  right: calc(100% + 12px);
}
.pw-sn-r {
  left: calc(100% + 12px);
}

/* ---- Page number (top-right header) ---- */
/* Page number: right-aligned line at top of each page */
.page-number {
  display: block;
  text-align: right;
  font-family: "Courier Prime", "Courier New", Courier, monospace;
  font-size: 12pt;
  line-height: 16px;
  margin-bottom: 16px; /* one blank line before content starts */
}

/* ---- Cover Page ---- */
.cover-page {
  position: relative;
  text-align: center;
}

.cover-title-block {
  padding-top: 3in;
}

.cover-title {
  font-size: 12pt;
  font-weight: bold;
  text-transform: uppercase;
  line-height: 16px;
  margin-bottom: 32px;
}

.cover-credit {
  font-size: 12pt;
  line-height: 16px;
  margin-bottom: 8px;
}

.cover-author {
  font-size: 12pt;
  line-height: 16px;
  margin-bottom: 16px;
}

.cover-based-on {
  font-size: 12pt;
  line-height: 16px;
  font-style: italic;
}

.cover-info-block {
  position: absolute;
  bottom: 1in;
  left: 1.5in;
  text-align: left;
}

.cover-info {
  font-size: 12pt;
  line-height: 16px;
}

/* ---- Screen preview: paginated like Google Docs ---- */
@media screen {
  html {
    background: #e8e8e8;
  }

  body {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
    padding: 40px 0 80px;
  }

  .page {
    width: 8.5in;
    min-height: 11in;
    padding: 1in 1in 1in 1.5in;
    background: #fff;
    box-shadow: 0 1px 4px rgba(0,0,0,0.1);
    position: relative;
  }

  /* After pagination runs, fix page heights */
  .page.paginated {
    height: 11in;
    overflow: hidden;
  }
}

/* ---- Print: each .page is a printed page ---- */
@media print {
  body {
    display: block;
    padding: 0;
  }

  .page {
    page-break-after: always;
    position: relative;
    width: auto;
    height: auto;
    padding: 0 0 0 0.5in; /* extra left padding so text starts at 1.5in from page edge */
    box-shadow: none;
    overflow: visible;
  }

  /* Cover page needs fixed dimensions in print so absolute positioning works */
  .page.cover-page {
    height: 9in; /* content area after @page margins */
    padding: 0;
  }

  .page.cover-page .cover-info-block {
    bottom: 0;
    left: 0;
  }

  .page:last-child {
    page-break-after: auto;
  }

  .page-number {
    margin-bottom: 16px;
  }
}
</style>
</head>
<body>
${e}
<script>
// Paginate after fonts are ready
document.fonts.ready.then(function() {
  requestAnimationFrame(function() {
    requestAnimationFrame(function() {
      paginate();
    });
  });
});

function paginate() {
  var body = document.body;
  var coverPage = body.querySelector('.cover-page');
  var allElements = Array.from(body.querySelectorAll('p[data-type]'));
  if (allElements.length === 0) return;

  // Remove all elements from DOM
  allElements.forEach(function(el) { el.remove(); });

  // Measure actual available content height using a test page
  var testPage = document.createElement('div');
  testPage.className = 'page';
  testPage.style.visibility = 'hidden';
  body.appendChild(testPage);
  var pageStyle = window.getComputedStyle(testPage);
  var CONTENT_H = testPage.clientHeight - parseFloat(pageStyle.paddingTop) - parseFloat(pageStyle.paddingBottom);
  if (CONTENT_H <= 0) CONTENT_H = 9 * 96; // fallback
  // Account for page number height
  var testNum = document.createElement('span');
  testNum.className = 'page-number';
  testNum.textContent = '1.';
  testPage.appendChild(testNum);
  var pageNumH = testNum.offsetHeight + parseFloat(window.getComputedStyle(testNum).marginBottom || '0');
  CONTENT_H -= pageNumH;
  testPage.remove();

  var pages = [];
  var startPageNum = coverPage ? 2 : 1;
  var currentPage = createPage(startPageNum);
  pages.push(currentPage);
  body.appendChild(currentPage);
  var accumulated = 0;

  function getType(el) {
    var cls = el.className || '';
    if (cls.includes('pm-character')) return 'pm-character';
    if (cls.includes('pm-dialogue')) return 'pm-dialogue';
    if (cls.includes('pm-parenthetical')) return 'pm-parenthetical';
    if (cls.includes('pm-scene-heading')) return 'pm-scene-heading';
    return '';
  }

  function newPage() {
    currentPage.classList.add('paginated');
    currentPage = createPage(pages.length + startPageNum);
    pages.push(currentPage);
    body.appendChild(currentPage);
    accumulated = 0;
  }

  var KEEP_WITH_PREV = ['pm-dialogue', 'pm-parenthetical'];

  for (var i = 0; i < allElements.length; i++) {
    var el = allElements[i];
    currentPage.appendChild(el);

    // Force layout then measure
    var rect = el.getBoundingClientRect();
    var style = window.getComputedStyle(el);
    var h = rect.height + (parseFloat(style.marginTop) || 0) + (parseFloat(style.marginBottom) || 0);

    if (h <= 0) { accumulated += 16; continue; } // fallback for unmeasurable elements

    if (accumulated + h > CONTENT_H && accumulated > 0) {
      el.remove();

      var type = getType(el);
      if (KEEP_WITH_PREV.indexOf(type) !== -1) {
        var pulled = [el];
        while (currentPage.lastElementChild && currentPage.lastElementChild.tagName === 'P') {
          var prev = currentPage.lastElementChild;
          var prevType = getType(prev);
          prev.remove();
          pulled.unshift(prev);
          if (prevType === 'pm-character') break;
          if (KEEP_WITH_PREV.indexOf(prevType) === -1) break;
        }
        newPage();
        pulled.forEach(function(p) { currentPage.appendChild(p); });
        accumulated = 0;
        Array.from(currentPage.querySelectorAll('p')).forEach(function(p) {
          accumulated += p.getBoundingClientRect().height +
            (parseFloat(window.getComputedStyle(p).marginTop) || 0) +
            (parseFloat(window.getComputedStyle(p).marginBottom) || 0);
        });
      } else {
        newPage();
        currentPage.appendChild(el);
        accumulated = h;
      }
    } else {
      accumulated += h;
    }
  }

  // Mark last page and clean up first-child margins
  currentPage.classList.add('paginated');
  pages.forEach(function(page) {
    var first = page.querySelector('p');
    if (first) first.style.marginTop = '0';
  });
}

function createPage(num) {
  var page = document.createElement('div');
  page.className = 'page';
  if (num > 1) {
    var pageNum = document.createElement('span');
    pageNum.className = 'page-number';
    pageNum.textContent = num + '.';
    page.appendChild(pageNum);
  }
  return page;
}
<\/script>
</body>
</html>`;
}
function Se(e) {
	let t = [];
	return t.push("<div class=\"page cover-page\">"), t.push("<div class=\"cover-title-block\">"), e.title && t.push(`<p class="cover-title">${X(e.title)}</p>`), e.author && (t.push("<p class=\"cover-credit\">Written by</p>"), t.push(`<p class="cover-author">${X(e.author)}</p>`)), e.basedOn && t.push(`<p class="cover-based-on">${X(e.basedOn)}</p>`), t.push("</div>"), t.push("<div class=\"cover-info-block\">"), e.draftDate && t.push(`<p class="cover-info">${X(e.draftDate)}</p>`), e.copyright && t.push(`<p class="cover-info">${X(e.copyright)}</p>`), e.contactInfo && t.push(`<p class="cover-info">${X(e.contactInfo)}</p>`), t.push("</div>"), t.push("</div>"), t.join("\n");
}
function Q(e, t = {}) {
	if (typeof window > "u") throw Error("exportToPDF requires a browser environment.");
	let n = Y(e), r = Z((t.coverPage ? Se(t.coverPage) : "") + n, t.title || t.coverPage?.title), i = window.open("", "_blank");
	if (!i) throw Error("Could not open print window. Please allow popups.");
	i.document.write(r), i.document.close(), i.onafterprint = () => {};
	let a = () => {
		!i || i.closed || (i.document.querySelectorAll(".page").length > 0 ? i.document.fonts ? i.document.fonts.ready.then(() => {
			setTimeout(() => i.print(), 200);
		}) : setTimeout(() => i.print(), 500) : setTimeout(a, 100));
	};
	setTimeout(a, 300);
}
function $(e, t) {
	return Z(Y(e), t);
}
//#endregion
//#region src/index.ts
var Ce = class {
	constructor(t) {
		this._readingMode = !1, this.options = t;
		let n = t.doc ? f.fromJSON(g, t.doc) : t.content ? B(t.content) : g.nodes.doc.create({}, [g.nodes.sceneHeading.create()]), r = [
			re,
			S,
			...t.history === !1 ? [] : [l({
				"Mod-z": c,
				"Mod-y": s,
				"Shift-Mod-z": s
			})],
			l(u),
			...t.history === !1 ? [] : [o()],
			ie,
			j,
			P()
		];
		t.bubbleMenu !== !1 && r.push(ce()), t.typeBadge !== !1 && r.push(k()), t.extraPlugins?.length && r.push(...t.extraPlugins);
		let i = e.create({
			doc: n,
			plugins: r
		});
		t.container.style.position = "relative", this.view = new a(t.container, {
			state: i,
			editable: () => !this._readingMode,
			handleDOMEvents: {
				dragstart: (e, t) => (t.preventDefault(), !0),
				dragover: (e, t) => (t.preventDefault(), !0),
				drop: (e, t) => (t.preventDefault(), !0)
			},
			handleDrop: () => !0,
			dispatchTransaction: (e) => {
				let n = this.view.state.apply(e);
				this.view.updateState(n), e.docChanged && t.onChange && t.onChange(n.doc);
			}
		}), t.attribution !== !1 && this.renderAttribution(t.container), t.darkMode && this.setDarkMode(!0);
	}
	renderAttribution(e) {
		let t = document.createElement("a");
		t.href = "https://plotwell.co", t.target = "_blank", t.rel = "noopener", t.className = "plotwell-attribution", t.textContent = "Powered by plotwell", e.appendChild(t);
	}
	replaceDoc(e) {
		let t = this.view.state.tr.replaceWith(0, this.view.state.doc.content.size, e.content);
		this.view.dispatch(t);
	}
	importFountain(e) {
		this.replaceDoc(B(e));
	}
	importFDX(e) {
		this.replaceDoc(U(e));
	}
	importScenes(e) {
		this.replaceDoc(W(e));
	}
	importJSON(e) {
		this.replaceDoc(f.fromJSON(g, e));
	}
	importTiptap(e) {
		this.replaceDoc(G(e));
	}
	toFountain() {
		return K(this.view.state.doc);
	}
	toFDX() {
		return J(this.view.state.doc);
	}
	toPDF(e) {
		Q(this.view.state.doc, e);
	}
	toPrintHTML(e) {
		return $(this.view.state.doc, e);
	}
	toJSON() {
		return this.view.state.doc.toJSON();
	}
	get readingMode() {
		return this._readingMode;
	}
	setReadingMode(e) {
		this._readingMode = e, this.view.dom.classList.toggle("pw-reading-mode", e), this.view.setProps({ editable: () => !this._readingMode });
	}
	setDarkMode(e) {
		let t = this.view.dom.closest(".plotwell-editor-wrap");
		t && t.classList.toggle("pw-dark", e);
	}
	getStats() {
		let e = this.view.state.doc, t = 0, n = 0, r = 0, i = 0;
		e.forEach((e) => {
			let a = e.textContent, o = a.trim() ? a.trim().split(/\s+/).length : 0;
			switch (e.type.name) {
				case "sceneHeading":
					t++, n += o, r += a.length;
					break;
				case "dialogue":
				case "parenthetical":
					i += o, n += o, r += a.length;
					break;
				default:
					n += o, r += a.length;
					break;
			}
		});
		let a = this.view.dom.querySelectorAll(".pw-page-separator").length + 1, o = n > 0 ? Math.round(i / n * 100) : 0;
		return {
			sceneCount: t,
			wordCount: n,
			characterCount: r,
			pageCount: a,
			dialoguePercentage: o
		};
	}
	destroy() {
		this.view.destroy();
	}
};
//#endregion
export { Ce as PlotwellEditor, J as docToFDX, K as docToFountain, ve as docToTiptap, Q as exportToPDF, U as fdxToDoc, B as fountainToDoc, W as scenesToDoc, g as schema, G as tiptapToDoc, $ as toPrintHTML };
